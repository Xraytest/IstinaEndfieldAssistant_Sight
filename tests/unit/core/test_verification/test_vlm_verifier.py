\"\"\"测试 verification_core/vlm_verifier.py - VLM 验证框架\"\"\"\nimport pytest\nimport json\nimport base64\nfrom unittest.mock import MagicMock, patch\n\nfrom src.core.verification_core.vlm_verifier import (\n    VerificationResult,\n    ModelEndpointManager,\n    VLMVerifier,\n)\n\n\nclass TestVerificationResult:\n    \"\"\"测试 VerificationResult 数据类\"\"\"\n\n    def test_create_minimal(self):\n        vr = VerificationResult(verified=True, confidence=0.95)\n        assert vr.verified is True\n        assert vr.confidence == 0.95\n        assert vr.reasoning == \"\"\n        assert vr.error is None\n\n\nclass TestModelEndpointManager:\n    \"\"\"测试 ModelEndpointManager\"\"\"\n\n    def test_init(self):\n        mgr = ModelEndpointManager()\n        assert \"qwen/qwen3.5-27b(free)\" in mgr.FREE_MODELS\n        assert mgr.current_model is None\n\n    def test_health_check_success(self):\n        mgr = ModelEndpointManager()\n        with patch(\"requests.post\") as mock_post:\n            mock_post.return_value.status_code = 200\n            mock_post.return_value.json.return_value = {\n                \"choices\": [{\"message\": {\"content\": \"2\"}}]\n            }\n            assert mgr.health_check(\"test-model\") is True\n\n    def test_health_check_failure(self):\n        mgr = ModelEndpointManager()\n        with patch(\"requests.post\") as mock_post:\n            mock_post.return_value.status_code = 200\n            mock_post.return_value.json.return_value = {\n                \"choices\": [{\"message\": {\"content\": \"maybe\"}}]\n            }\n            assert mgr.health_check(\"test-model\") is False\n\n    def test_health_check_timeout(self):\n        mgr = ModelEndpointManager()\n        with patch(\"requests.post\", side_effect=Exception(\"timeout\")):\n            assert mgr.health_check(\"test-model\") is False\n\n    def test_get_fallback_chain(self):\n        mgr = ModelEndpointManager()\n        chain = mgr.get_fallback_chain(\"qwen/qwen3.5-27b(free)\")\n        assert len(chain) >= 2\n        assert \"qwen/qwen3.5-27b(free)\" not in chain\n\n\nclass TestVLMVerifier:\n    def setup_method(self):\n        self.mock_mgr = MagicMock()\n        self.mock_mgr.url = \"https://open.cherryin.cc/v1\"\n        self.mock_mgr._headers.return_value = {\"Authorization\": \"Bearer test-key\"}\n        self.mock_mgr.get_fallback_chain.return_value = [\"qwen/qwen3-vl-plus\"]\n        self.mock_mgr.current_model = \"qwen/qwen3.5-27b(free)\"\n        self.verifier = VLMVerifier(endpoint_mgr=self.mock_mgr)\n\n    def test_init_uses_provided_endpoint(self):\n        assert self.verifier.current_model == \"qwen/qwen3.5-27b(free)\"\n\n    def test_call_vlm_success(self):\n        with patch(\"requests.post\") as mock_post:\n            mock_post.return_value.status_code = 200\n            mock_post.return_value.json.return_value = {\n                \"choices\": [{\"message\": {\"content\": '{\"matches_expected\": true}'}}]\n            }\n            result = self.verifier._call_vlm(\"fake_b64\", \"test prompt\", \"test-model\")\n            assert \"error\" not in result\n            assert result[\"content\"] == '{\"matches_expected\": true}'\n\n    def test_call_vlm_http_error(self):\n        with patch(\"requests.post\") as mock_post:\n            mock_post.return_value.status_code = 401\n            mock_post.return_value.text = \"Unauthorized\"\n            result = self.verifier._call_vlm(\"fake_b64\", \"test\", \"test-model\")\n            assert \"error\" in result\n\n    def test_parse_json_braces(self):\n        text = 'Some text {\"key\": \"value\"} more text'\n        parsed = self.verifier._parse_json(text)\n        assert parsed is not None\n        assert parsed[\"key\"] == \"value\"\n\n    def test_parse_json_markdown(self):\n        text = '```json\\n{\"key\": \"value\"}\\n```'\n        parsed = self.verifier._parse_json(text)\n        assert parsed is not None\n        assert parsed[\"key\"] == \"value\"\n\n    def test_parse_json_invalid(self):\n        assert self.verifier._parse_json(\"no json here\") is None\n\n    def test_call_with_retry_first_success(self):\n        with patch.object(self.verifier, \"_call_vlm\") as mock:\n            mock.return_value = {\"content\": '{\"ok\": true}'}\n            result = self.verifier._call_with_retry(\"b64\", \"prompt\")\n            assert \"error\" not in result\n            assert result[\"content\"] == '{\"ok\": true}'\n\n    def test_call_with_retry_all_fail(self):\n        with patch.object(self.verifier, \"_call_vlm\") as mock:\n            mock.return_value = {\"error\": \"failed\"}\n            result = self.verifier._call_with_retry(\"b64\", \"prompt\", max_retries=2)\n            assert \"error\" in result\n\n    def test_verify_task_progress_success(self):\n        with patch.object(self.verifier, \"_call_with_retry\") as mock_call:\n            mock_call.return_value = {\n                \"content\": json.dumps({\n                    \"matches_expected\": True,\n                    \"current_progress\": \"10/10\",\n                    \"numeric\": [10, 10],\n                    \"state\": \"completed\",\n                    \"vlm_confidence\": 0.95,\n                    \"explanation\": \"Task progress shows 10/10\"\n                })\n            }\n            result = self.verifier.verify_task_progress(\"b64\", \"10/10\")\n            assert result.verified is True\n            assert result.confidence == 0.95\n            assert result.extracted_data[\"numeric\"] == [10, 10]\n\n    def test_verify_task_progress_not_met(self):\n        with patch.object(self.verifier, \"_call_with_retry\") as mock_call:\n            mock_call.return_value = {\n                \"content\": json.dumps({\n                    \"matches_expected\": False,\n                    \"current_progress\": \"3/10\",\n                    \"numeric\": [3, 10],\n                    \"state\": \"in_progress\",\n                    \"vlm_confidence\": 0.9,\n                })\n            }\n            result = self.verifier.verify_task_progress(\"b64\", \"10/10\")\n            assert result.verified is False\n            assert result.extracted_data[\"numeric\"] == [3, 10]\n\n    def test_verify_reward_popup_found(self):\n        with patch.object(self.verifier, \"_call_with_retry\") as mock_call:\n            mock_call.return_value = {\n                \"content\": json.dumps({\n                    \"has_reward_popup\": True,\n                    \"reward_items\": [{\"name\": \"金币\", \"amount\": \"x100\"}],\n                    \"explanation\": \"Reward popup visible\"\n                })\n            }\n            result = self.verifier.verify_reward_popup(\"b64\", [\"金币\"])\n            assert result.verified is True\n            assert result.confidence > 0.5\n\n    def test_verify_reward_popup_not_found(self):\n        with patch.object(self.verifier, \"_call_with_retry\") as mock_call:\n            mock_call.return_value = {\n                \"content\": json.dumps({\n                    \"has_reward_popup\": False,\n                    \"reward_items\": [],\n                    \"explanation\": \"No reward popup\"\n                })\n            }\n            result = self.verifier.verify_reward_popup(\"b64\")\n            assert result.verified is False\n\n    def test_verify_combat_victory_true(self):\n        with patch.object(self.verifier, \"_call_with_retry\") as mock_call:\n            mock_call.return_value = {\n                \"content\": json.dumps({\n                    \"is_combat_ended\": True,\n                    \"is_victory\": True,\n                    \"is_defeat\": False,\n                    \"visual_cues\": [\"勝利\", \"Victory\"],\n                    \"confidence\": 0.98,\n                })\n            }\n            result = self.verifier.verify_combat_victory(\"b64\")\n            assert result.verified is True\n            assert result.confidence == 0.98\n\n    def test_verify_combat_victory_false(self):\n        with patch.object(self.verifier, \"_call_with_retry\") as mock_call:\n            mock_call.return_value = {\n                \"content\": json.dumps({\n                    \"is_combat_ended\": False,\n                    \"is_victory\": False,\n                    \"is_defeat\": False,\n                })\n            }\n            result = self.verifier.verify_combat_victory(\"b64\")\n            assert result.verified is False\n\n    def test_verify_vlm_error_handling(self):\n        with patch.object(self.verifier, \"_call_with_retry\") as mock_call:\n            mock_call.return_value = {\"error\": \"all models failed\"}\n            result = self.verifier.verify_task_progress(\"b64\", \"10/10\")\n            assert result.verified is False\n            assert result.error == \"all models failed\"\n\n    def test_verify_parse_failure(self):\n        with patch.object(self.verifier, \"_call_with_retry\") as mock_call:\n            mock_call.return_value = {\"content\": \"This is not JSON at all\"}\n            result = self.verifier.verify_task_progress(\"b64\", \"10/10\")\n            assert result.verified is False\n            assert \"Failed to parse\" in result.reasoning\n\n    def test_call_with_retry_uses_reasoning_when_content_empty(self):\n        with patch.object(self.verifier, \"_call_vlm\") as mock_call:\n            mock_call.return_value = {\n                \"content\": \"\",\n                \"reasoning\": '{\"matches_expected\": true, \"current_progress\": \"10/10\"}',\n            }\n            result = self.verifier._call_with_retry(\"b64\", \"prompt\")\n            assert result[\"content\"] == '{\"matches_expected\": true, \"current_progress\": \"10/10\"}'\n\n    def test_call_vlm_with_image_format(self):\n        with patch(\"requests.post\") as mock_post:\n            mock_post.return_value.status_code = 200\n            mock_post.return_value.json.return_value = {\n                \"choices\": [{\"message\": {\"content\": \"ok\"}}]\n            }\n            result = self.verifier._call_vlm(\"base64data\", \"test\", \"qwen/qwen3.5-27b(free)\")\n            _, kwargs = mock_post.call_args\n            body = kwargs[\"json\"]\n            assert body[\"messages\"][0][\"content\"][0][\"type\"] == \"image_url\"\n            assert \"data:image/png;base64,base64data\" in body[\"messages\"][0][\"content\"][0][\"image_url\"][\"url\"]\n", "filePath": "C:\\Users\\xray\\Documents\\ArkStudio\\IstinaAI\\IstinaEndfieldAssistant\\tests\\unit\\core\\test_verification\\test_vlm_verifier.py"}