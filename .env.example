# =============================================================================
# IstinaEndfieldAssistant 环境变量示例
# =============================================================================
# 复制本文件为 .env 并填入真实值。.env 已被 .gitignore 忽略，不会提交。
#
# 注意：当启用「设置 → 启用开发者设置」后，开发者选项中的 LLM 后端切换与
# 云端 API 配置会由 GUI 自动写入 .env（仅 LLM_PROVIDER / LLM_CLOUD_* 字段），
# 手动编辑的其它字段会被保留。
#
# LLM/VLM 后端互斥切换：
#   - provider=local  : 使用本地 llama-server（默认，无需下方云端变量）
#   - provider=cloud  : 使用云端 OpenAI 兼容 API（需配置下方云端变量）
# =============================================================================

# LLM 后端选择：local | cloud
# - local: 启动 3rd-part/llama-cpp/llama-server.exe 加载本地 GGUF 模型
# - cloud: 直接调用云端 API，不启动本地 llama-server
LLM_PROVIDER=local

# 云端 OpenAI 兼容 API 配置（仅 LLM_PROVIDER=cloud 时生效）
# base_url 应包含 /v1 后缀（OpenAI 兼容协议）
# 替换为你自己的云端 LLM 端点地址
LLM_CLOUD_BASE_URL=https://your-cloud-llm-endpoint/v1
LLM_CLOUD_API_KEY=
LLM_CLOUD_MODEL=qwen/qwen3.5-35b-a3b(free)
