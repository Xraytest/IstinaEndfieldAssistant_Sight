# IstinaEndfieldAssistant Sight 运行时依赖
# Python >= 3.10

# GUI 框架
PyQt6>=6.5.0

# 图像处理
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# 加密与通信
cryptography>=41.0.0
requests>=2.31.0
aiohttp>=3.9.0

# 视频流处理
av>=10.0.0

# ADB 设备控制
adbutils>=2.0.0

# 本地 VLM 推理（可选，本地模式需要）
llama-cpp-python>=0.2.0

# MaaFramework 触控/OCR（可选，MaaFw 模式需要）
MaaFw>=1.0.0

# 测试依赖（可选）
pytest>=7.0.0
