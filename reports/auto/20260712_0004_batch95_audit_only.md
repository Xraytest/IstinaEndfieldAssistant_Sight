# 审计批次 95 — 批次 92~94 审计复核 / 无新发现

**生成时间**: 2026-07-12 00:04
**覆盖文件**: `reports/auto/20260711_2331_batch92_metadata_cache_atomic.md`, `reports/auto/20260711_2342_batch93_llm_stop_disconnect_silent.md`, `reports/auto/20260711_2351_batch94_android_drain_scrcpy_silent.md`
**审计方法**: 静态代码逻辑分析，无测试执行
**前置去重**: 已读取 `reports/CODE_REVIEW_WARNS.md`，新发现跳过 FP-01~08、FX-01~10、DUP-A~J、O-01~O-24

---

## 审计结论（批次 92~94）

| 批次 | 审计对象 | 结论 |
|------|---------|------|
| 批次 92 | CACHE-01 (`_persist_metadata_cache` 非原子写入) | **结论正确**。`maaend_control_page.py:1575-1584` 直接 `write_text` 覆盖写入，异常时文件截断损坏。同文件内至少 3 处非原子写入（`_export_queue`、`_write_config`、`_persist_metadata_cache`），而 `settings_page.py` 和 `queue_state.py` 已使用原子写入（tempfile + os.replace）。 |
| 批次 93 | LLM-02 (`llm start` 无诊断信息) | **结论正确**。`cli/handlers.py:239-240` 在 `warmup_llm()` 返回 False 时仅返回 `{"status": "error"}` 不含 message 字段，而 `LlamaServerRuntime.start()` 内部记录详细失败原因但不暴露。 |
| 批次 93 | LLM-03 (`llm stop` 总是成功) | **结论正确**。`cli/handlers.py:242-243` 无条件返回 success，`cooldown_llm()` 内部 `except Exception: warning` 吞掉异常，handler 无法感知失败。 |
| 批次 93 | SYS-01 (`disconnect` 总是成功) | **结论正确**。`cli/handlers.py:116-117` 无条件返回 success，`runtime.disconnect()` 内部 `except Exception: error` 吞掉异常，handler 无法感知失败。 |
| 批次 94 | NAV-DRAIN (`_drain_pipe` / `_accept_loop` / `_handle_client` 守护线程未 join) | **结论正确**。`android_runtime.py:233, 494, 534` 创建的 daemon 线程在 `stop()` 时仅 join 主线程，守护线程持有的 fd 在进程生命周期内持续累积。 |
| 批次 94 | SCRCPY-SILENT (`connect()` 无条件返回 True 即使 scrcpy 失败) | **结论正确**。`runtime.py:222-254` 在 scrcpy 预览通道启动失败时仅记录 warning，最终 `return True`，GUI 侧无法感知 scrcpy 未启动。 |
| 批次 94 | AUTO-PARAMS (`_resolve_connect_params` 裸 except 吞错) | **结论正确**。`maaend_control_page.py:412-424` 在读取 `client_config.json` 时捕获所有异常并静默返回空 dict，自动连接无诊断。 |
| 批次 94 | PLAYER-SILENT (脚本回放动作失败静默继续) | **结论正确**。`scripting/player.py:135-138` 在动作执行失败时仅记录 warning，继续执行后续动作，用户不知情。 |

**批次 92~94 全部新发现经本批次逐项源码复核确认准确，无需修正。**

---

## 新增发现（0 项）

本轮审计未发现新的、未被 CODE_REVIEW_WARNS.md 覆盖的问题。

### 候选点分析（均已排除）

| 候选 | 排除原因 |
|------|----------|
| `_clear_canvas` 3 处 bare `except: pass` (recovery.py:85,89,93) | **设计意图**：应用重启清理流程，单个步骤失败不应中断后续清理。属于"尽力清理"模式，非 bug。 |
| `_extract_features` bare `except: return {}` (recognizer.py:431) | **DUP-D 模式**：已列入 CODE_REVIEW_WARNS.md §4 为重复主题（I18N 静默失败/缺键不追踪）。 |
| `find_by_name("")` 匹配全部实体 (entity_db.py:150) | **O-19**：已列入 CODE_REVIEW_WARNS.md §6 为有效 Open 项。 |
| `widget_styles.py` BLUE_STYLE 重复定义 (line 49, 57) | **DUP-B**：硬编码冗余，已列入 CODE_REVIEW_WARNS.md §4。后定义覆盖前定义，无运行时差异。 |
| `_drain_pipe` daemon 线程 join 缺失 | **批次 94 已报告**：NAV-DRAIN 涵盖此问题。 |
| `connect()` 无条件返回 True | **批次 93/94 已报告**：SYS-01 和 SCRCPY-SILENT 已涵盖。 |
| `_resolve_connect_params` bare except | **批次 94 已报告**：AUTO-PARAMS 已涵盖。 |
| `_frame_to_base64` 无 cv2.imencode 检查 | **O-22**：已列入 CODE_REVIEW_WARNS.md §6。 |
| `_start_agent` go-service stdout/stderr 重定向 DEVNULL | **O-23**：已列入 CODE_REVIEW_WARNS.md §6。 |
| `adb_manager` adbutils 失败静默回退 | **O-24**：已列入 CODE_REVIEW_WARNS.md §6。 |

---

## 审计方法说明

本轮审计覆盖以下文件：
- `reports/auto/` 批次 92~94 全部报告
- `core/capability/device/recovery.py`
- `core/foundation/shell_security.py`
- `core/foundation/gpu_check.py`
- `core/foundation/paths.py`
- `gui/pyqt6/responsive.py`
- `gui/pyqt6/scripting/recorder.py`
- `gui/pyqt6/scripting/player.py`
- `gui/pyqt6/theme/theme_manager.py`
- `gui/pyqt6/theme/widget_styles.py`
- `gui/pyqt6/i18n/__init__.py`
- `core/capability/element_recognition/backends/template_backend.py`
- `core/capability/element_recognition/backends/color_backend.py`
- `core/capability/element_recognition/backends/yolo_backend.py`
- `core/capability/element_recognition/backends/ocr_backend.py`
- `core/capability/element_recognition/backends/scene_geometry.py`
- `core/capability/element_recognition/tasks/task_runner.py`

所有发现均已对照 CODE_REVIEW_WARNS.md 去重，无重复提交。

---

*批次 95 报告 | 仅分析，无文件修改*
